{
  "name": "devgn-cinex-pricing",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build",
    "postinstall": "PLAYWRIGHT_BROWSERS_PATH=0 npm --prefix backend ci --omit=dev && PLAYWRIGHT_BROWSERS_PATH=0 npm --prefix scraper ci --omit=dev && cd scraper && PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install chromium",
    "start": "node backend/index.js",
    "scrape:all": "npm --prefix scraper run scrape:all"
  }
}
